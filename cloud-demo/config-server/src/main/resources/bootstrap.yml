spring:
  application:
    name: config-server
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/xuth1018/cloud-demo
          search-paths: config
          username: 2624235265@qq.com
          password: qq101896
          basedir: E://config
      profile: git #默认git